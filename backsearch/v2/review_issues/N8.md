# N8: The collective page hard-codes the campaign-#1 question, class and 'six exit positions'

verdict CONFIRMED, severity low, category docs
locations: /Users/george/PathologyRecords/server/public/collective.html:14

## Summary
The landing page's subtitle and 'What comes out' text are static strings for 5x5 <=3 holes, 149 moves and six exits. They are not derived from /api/v2/status, so any next campaign shows the wrong question and class until someone hand-edits the HTML. 5x6 would have nine exits.

## Source report F207 (r2-next-campaign-generalization)
**The collective page hard-codes the campaign-#1 question, class and 'six exit positions'** at /Users/george/PathologyRecords/server/public/collective.html:14

*summary*: The landing page's subtitle and 'What comes out' text are static strings for 5x5 <=3 holes, 149 moves and six exits. They are not derived from /api/v2/status, so any next campaign shows the wrong question and class until someone hand-edits the HTML. 5x6 would have nine exits.

*failure_scenario*: The owner seeds a 4x6 or 5x5 <=4-hole campaign and forgets to edit collective.html. Volunteers read 'is 149 the longest 5x5 level with at most 3 holes?' and 'When all six exit positions are covered ... the longest 5x5 level with at most 3 holes' while their machines search a different class. On 5x6, the 'six exit positions' text contradicts the nine-row exit table below it.

*evidence*: collective.html:14 `is 149 the longest 5x5 level with at most 3 holes? Lend some cores and find out.`; collective.html:85-86 `When all six exit positions are covered, the result is an exhaustive proof: the longest\n        5x5 level with at most 3 holes`.

*suggested_fix*: Fill these sentences from the status payload in collective.js: campaign title, grid, caps from extra (as campaigns.js already does with its regex), and exits.length. Or add a 'question' field to the campaign plan.

## Verifier (CONFIRMED, low, refuted=False)
The page text is static. collective.html:14 reads 'is 149 the longest 5x5 level with at most 3 holes? Lend some cores and find out.' and collective.html:85-86 reads 'When all six exit positions are covered ... the longest 5x5 level with at most 3 holes'. collective.js never touches the .sub paragraph or the #how section. load() only fills #totals, #exitTable, #bestLevels and #clients, and when no campaign is active it prints 'No search is running right now' under the unchanged question. The subtitle is already stale today: updates.json announces 'Proven: 149 is the longest 5x5 level with at most 3 holes' while the header still asks the question.

More hard-coded per-campaign values in the same page:
- 'After 30 minutes a worker stops' (split_after_s);
- 'lends a job for an hour' (lease_s);
- 'keeps your jobs reserved for up to 12 hours' (paused_max_s);
- 'About 2 % of finished jobs are quietly re-run' (dup_fraction);
- 'about 200 MB of memory' per worker, which may grow with more holes or a larger grid.

The status payload already carries title, grid, extra, exits, split_after_s, lease_s, paused_max_s and dup_fraction (jobs.js:321-327), so all of these could be derived. The impact is misleading copy only.

*fix notes*: Give the subtitle and the 'What comes out' / 'Long jobs split' / 'Leases' / Pause sentences spans with ids. Fill them in load() from S.campaign:
- grid;
- caps from extra (reuse the capsFromExtra-style regex that campaigns.js uses);
- exits.length;
- split_after_s/60, lease_s/3600, paused_max_s/3600, and dup_fraction*100.
When no campaign is active, show a neutral subtitle. Optionally add a 'question' string to the campaign plan in v2_seed.
