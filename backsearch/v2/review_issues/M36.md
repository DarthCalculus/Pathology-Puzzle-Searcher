# M36: Volunteer onboarding texts give a wrong run path, link the retired guide and the stale v2 branch, and misdescribe verification

verdict CONFIRMED, severity low, category docs
locations: hunt.js:44 / hunt.html (`cd .../backsearch` then `python3 volunteer.py`, but the file is v2/volunteer.py; 'Full guide' links the retired chunk-tracker VOLUNTEERS.md); collective.html (links README/DESIGN on branch v2, 4 commits behind main and missing §9/release-by-omission; 'every reported level is re-solved'; campaign-1 text hard-coded: 'is 149 the longest', 'six exit positions'); VOLUNTEERS.md fallback build; README default workers; collective.js b.done_at

## Summary
hunt.html has volunteers cd into backsearch/ and run `python3 volunteer.py`, but the file is v2/volunteer.py, and its 'Full guide' goes to the retired chunk-tracker VOLUNTEERS.md. collective.html links the README and DESIGN on the stale `v2` branch, which lacks the §9 and release-by-omission changes. It says every level is re-solved (only new per-exit bests are) and hard-codes campaign-1 text, while the snapshot still shows campaign #1 as 'open', so the page invites volunteers to a finished search. F198 also repeats the fallback-build, default-worker and done_at issues filed separately.

## Source report F118 (server-web)
**Volunteer instructions on the pages point to a wrong path, the retired guide and a stale branch, and describe verification incorrectly** at /Users/george/PathologyRecords/server/public/hunt.js:44

*summary*: hunt.html has users cd into backsearch/ and then run `python3 volunteer.py`, but the file is v2/volunteer.py. Its 'Full guide' link goes to VOLUNTEERS.md, which describes the retired chunk tracker. collective.html links README and DESIGN on branch v2, which is 4 commits behind main and lacks the §9 and release-by-omission changes (38 lines of DESIGN.md differ). It also says 'Every reported level is re-solved by the site', but only a new per-exit best is (§7.8). Its header ('is 149 the longest ...? Lend some cores and find out') and 'six exit positions' text are hardcoded for campaign #1. Campaign #1 is still status 'open' in the snapshot, so the page invites volunteers to a finished search.

*failure_scenario*: A volunteer follows hunt.html: `git clone ... && cd Pathology-Puzzle-Searcher/backsearch && ./build_pgo.sh ...`, then `python3 volunteer.py ...` fails with 'No such file'. A volunteer following the README/DESIGN links reads outdated protocol docs. When campaign #2 (e.g. 4 holes) starts, the landing page still says 5x5 / 3 holes / 149.

*evidence*: hunt.js:44 `$('cmdRun').textContent = `python3 volunteer.py --name "Your name" --workers 4 --server ${location.origin}`;` vs collective.js:41 `python3 v2/volunteer.py ...`
hunt.html:28 `.../blob/main/backsearch/VOLUNTEERS.md` (starts: 'the chunk tracker described below is retired')
collective.html:54-55 `.../blob/v2/backsearch/v2/README.md` / `DESIGN.md`; `git rev-parse`: v2=c5bf7d8, main=aa7f0bf
collective.html:83-84 `Every reported level is re-solved by the site.` vs v2routes.js:149 `§7.8: re-solve only a new per-exit best`
collective.html:14 `is 149 the longest 5x5 level with at most 3 holes? Lend some cores and find out.`

*suggested_fix*: Use `python3 v2/volunteer.py` in hunt.js and link v2/README.md on main. Point all links at main. Reword to 'every new per-exit best is re-solved by the site'. Render the header and class text from S.campaign (title, grid, exits.length), or from campaigns.html when no campaign is active. Close campaign #1 (v2_seed --close-others does this when campaign #2 is seeded).

## Source report F198 (robustness-ops)
**Onboarding docs have wrong or stale commands and links** at /Users/george/PathologyRecords/server/public/hunt.js:44

*summary*: Several onboarding texts mislead volunteers. (a) hunt.html's run command is `python3 volunteer.py` right after `cd .../backsearch`, but the file is v2/volunteer.py, and its 'Full guide' link points to the retired chunk-tracker VOLUNTEERS.md. (b) VOLUNTEERS.md's fallback build does not define SRC_HASH_STR, so the worker prints an empty hash and the client says the worker is 'too old; git pull'. (c) README.md says the default is core count minus one, but the code uses half the cores. (d) collective.html links to the stale `v2` branch and hard-codes campaign-1 text; collective.js reads `b.done_at`, which status never sends (it sends `at`), so 'found ... ago' never shows.

*failure_scenario*: (a) A volunteer copies both commands from hunt.html and gets "python3: can't open file 'volunteer.py'". (b) A volunteer whose PGO build failed uses VOLUNTEERS.md's documented plain build, and the client refuses it with 'did not print an SRC_HASH line ... too old; update the checkout and rebuild: git pull && ./build_pgo.sh', which will not help if build_pgo.sh is what fails. (d) For campaign #2 the landing page still asks 'is 149 the longest 5x5 level with at most 3 holes?'.

*evidence*: hunt.js:44 `python3 volunteer.py --name "Your name" ...`, next to hunt.html:24 `cd Pathology-Puzzle-Searcher/backsearch` and hunt.html:28 `.../blob/main/backsearch/VOLUNTEERS.md`. VOLUNTEERS.md:35 `cc -O3 -o backsearch_worker_nt backsearch.c sokoban_bfs.c nn_stub.c -lz -lm`, with backsearch.c:500-501 `#ifndef SRC_HASH_STR / #define SRC_HASH_STR ""` and volunteer.py:291-295 (HEX_RE needs 8+ hex chars). README.md:22-23 'Default is your core count minus one' vs volunteer.py:1531 `default=max(1, (os.cpu_count() or 2) // 2)`. collective.html:14, 54-55, 85-86. collective.js:98 `b.done_at` vs jobs.js:907 `best_level = { ..., at: b.done_at }`.

*suggested_fix*: hunt.js: use `python3 v2/volunteer.py`, and link v2/README.md. VOLUNTEERS.md: replace the body with a pointer to the v2 instructions, or give a fallback that passes `-DSRC_HASH_STR="$(cat backsearch.c sokoban_bfs.c sokoban_bfs.h | shasum -a 256 | cut -c1-64)"`. Align the workers default with the docs. Build the collective.html headline from /api/v2/campaign, link main, and read `b.at`. Mention that python.org macOS Python needs 'Install Certificates.command'.

## Verifier (CONFIRMED, low, refuted=False)
Every sub-claim checks out. (1) hunt.html:24 sets up with `cd Pathology-Puzzle-Searcher/backsearch && ./build_pgo.sh ...`, then hunt.js:44 sets the run command to `python3 volunteer.py --name ...`. There is no backsearch/volunteer.py (ls shows only VOLUNTEERS.md at that level); the file is backsearch/v2/volunteer.py. collective.js:41 has it right (`python3 v2/volunteer.py`), so only hunt.html, the 'full dashboard' linked from collective.html:64, is wrong. (2) hunt.html:28 links blob/main/backsearch/VOLUNTEERS.md, whose body is the retired chunk-tracker guide; only its banner points to v2/README.md. (3) collective.html:54-55 link blob/v2/.../README.md and DESIGN.md. In the local clone origin/v2=c5bf7d8 and origin/main=aa7f0bf, 4 commits apart (lease renewal, holding, release-by-omission, §9), and DESIGN.md differs by +34/-4 lines. (4) collective.html:83-84 says 'Every reported level is re-solved by the site', but v2routes.js:149 re-solves only a new per-exit best (§7.8). (5) The campaign #1 text is hard-coded: collective.html:14 asks 'is 149 the longest 5x5 level with at most 3 holes?' and collective.html:85-86 says 'all six exit positions ... 5x5 level with at most 3 holes'. In the snapshot, campaign 1 is still status 'open' (the live DB may differ). (6) F198 extras. VOLUNTEERS.md:35's fallback `cc -O3 ... backsearch.c sokoban_bfs.c nn_stub.c -lz -lm` does not define SRC_HASH_STR, so it falls back to backsearch.c:500-501 `#define SRC_HASH_STR ""`. The worker prints an empty hash, volunteer.py:291-295 (HEX_RE `^[0-9a-fA-F]{8,128}$`) exits with 'too old; git pull && ./build_pgo.sh', and that advice is useless when build_pgo.sh is what failed. The hash is of the sources, not the binary, so a plain build given the right -D would be accepted. v2/README.md:22 says the default is 'your core count minus one', but volunteer.py:1531 uses `max(1, (os.cpu_count() or 2) // 2)`. None of this affects results. It matters for the next campaign because the landing page will misstate the question and the rules, and hunt.html's command fails outright, so it stays low.

*fix notes*: hunt.js:44 should use `python3 v2/volunteer.py`, and hunt.html:28 should link v2/README.md on main. Point the collective.html:54-55 links at main, or delete the v2 branch. Reword collective.html:84 to 'every new per-exit best is re-solved by the site'. Render the collective.html header and the 'What comes out' text from S.campaign (title, grid, exits.length, holes), with a generic fallback when there is no campaign. VOLUNTEERS.md:35 fallback: add `-DSRC_HASH_STR="$(cat backsearch.c sokoban_bfs.c sokoban_bfs.h | shasum -a 256 | cut -c1-64)"`, or replace the file's body with a pointer to v2/README.md. Make README.md:22 and volunteer.py:1531 agree. Close campaign #1 (v2_seed --close-others) before campaign #2 opens.
