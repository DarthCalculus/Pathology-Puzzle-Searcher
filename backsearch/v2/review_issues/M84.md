# M84: Owner tooling and docs still default to individual names in proof attributions

verdict CONFIRMED, severity low, category docs
locations: README.md:48 and add_proof.js usage examples ('George Spahn'); finish_chunks.js default 'Collective effort: <names>'

## Summary
The README and add_proof.js usage examples publish proofs as 'George Spahn', and the retired finish_chunks.js defaults to 'Collective effort: <names>'. The owner's rule is 'Panacea' for his own proofs and 'Collective' for campaign proofs, so copying the examples produces wrong attributions.

## Source report F121 (server-web)
**Owner tooling and docs still default to individual names in proof attributions** at /Users/george/PathologyRecords/README.md:48

*summary*: The README and add_proof.js usage examples publish proofs as 'George Spahn', and the retired finish_chunks.js defaults to 'Collective effort: <names>'. The owner's rule is 'Panacea' for his own proofs and 'Collective' for campaign proofs.

*failure_scenario*: An owner or agent copies the README or add_proof.js example to add a proof, or runs finish_chunks.js without --attribution. The public champion line in app.js (`longest <class> — <attribution>`) then shows 'George Spahn', or a list of individual names, on the site.

*evidence*: README.md:48 `--attribution "George Spahn" --method "exhaustive backward search" --states 1042307497 --cpu 3780`
tools/add_proof.js:14 `--attribution "George Spahn" --method ...`
tools/finish_chunks.js:14-15 `|| `Collective effort: ${names.join(', ')}`;`
app.js:127 `(c.attribution ? ` — ${c.attribution}` : '')`

*suggested_fix*: Change the examples to "Panacea" and the finish_chunks default to 'Collective' (or delete the retired tool).

## Verifier (CONFIRMED, low, refuted=False)
Confirmed as written:
- README.md:48 `--attribution "George Spahn"`
- tools/add_proof.js:14 has the same example. Line 19 of that file already shows `--set-attribution 3 --attribution "Panacea"`.
- tools/finish_chunks.js:14-15 defaults to `Collective effort: ${names.join(', ')}`.
- app.js:127 prints the attribution in the public champion line.

The owner's rules are recorded: 'Panacea' for his own proofs (memory project_backsearch.md, 2026-09-15 feedback; DB attributions were already fixed once on 2026-09-23), and 'Collective' for campaign proofs (tools/v2_finish.js:48 `flag('--attribution') || 'Collective'; // owner's rule`).

Exposure for the next campaign is small. The v2 publishing path (v2_finish.js) already defaults correctly. finish_chunks.js is the retired v1 chunk tool and exits unless every chunk of a chunk plan is done. The only realistic misuse is copying the README or add_proof example for a hand-entered proof. README also does not document v2_finish.js at all, so an operator looking for how to publish lands on the add_proof example.

*fix notes*: Change README.md:48 and add_proof.js:14 to --attribution "Panacea". Change the finish_chunks.js default to 'Collective', or delete or clearly mark the retired v1 tool. Add a short README section for the v2 owner tools (v2_seed.js, v2_hashes.js, v2_finish.js [--dry], plus a new mismatch-resolution tool) that states the attribution rule.
